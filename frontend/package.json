{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "data:indexes": "python ../backend/run_scrapers.py",
    "data:sync": "npm run data:indexes && python ../backend/data_pipeline.py && python ../backend/analytics/train_regime_offset.py && powershell -Command \"Copy-Item ../backend/data/dashboard_data*.json -Destination public/ -Force; Copy-Item ../backend/data/regime_params.json -Destination public/ -Force; Copy-Item ../backend/data/etf_data.json -Destination public/ -Force; if (Test-Path ../backend/data/domains) { Copy-Item ../backend/data/domains -Destination public/ -Recurse -Force }\"",
    "dev": "npm run data:sync && vite",
    "build": "npm run data:sync && vite build",
    "build:quick": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "lightweight-charts": "^5.1.0",
    "plotly.js-dist-min": "^3.3.1"
  }
}